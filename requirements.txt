# Pinned 2026-09-11 on Python 3.11 (conda env `neuro-signatures`). See CLAUDE.md §10/§16.
#
# NOTE 1: setuptools must stay <81 — abagen's mouse submodule imports pkg_resources,
# which setuptools>=81 dropped. Without this pin, `import abagen` fails with
# `ModuleNotFoundError: No module named 'pkg_resources'`.
#
# NOTE 2: pandas must stay ==1.5.3 — abagen 0.1.3 (latest on PyPI, unmaintained past this)
# calls BOTH `DataFrame.groupby(sid, axis=1)` (axis kwarg removed in pandas 2.2) AND
# `df.set_axis(symbols, axis=1, inplace=False)` (inplace kwarg removed in pandas 2.0).
# 1.5.3 is the newest release compatible with both call sites. Without this pin,
# `abagen.get_expression_data(...)` fails with either
# `TypeError: DataFrame.groupby() got an unexpected keyword argument 'axis'` or
# `TypeError: DataFrame.set_axis() got an unexpected keyword argument 'inplace'`.
setuptools<81
pandas==1.5.3

numpy==1.26.4
scipy==1.17.1
statsmodels==0.15.0
scikit-learn==1.9.1
matplotlib==3.11.1
pyyaml==6.0.3
requests==2.34.2

abagen==0.1.3
neuromaps==0.0.7

# Not on PyPI — install from source:
#   pip install "git+https://github.com/MICA-MNI/ENIGMA.git"
enigmatoolbox==2.0.3

# Optional (§10, brain-surface plotting). NOTE: nilearn 0.14.1's stated requirement is
# pandas>=2.3.0, which conflicts with the pandas==1.5.3 pin above (`pip check` will flag
# this). In practice nilearn imports and runs fine under pandas 1.5.3 for the plotting
# calls this project uses — if that changes, drop nilearn and plot with matplotlib only.
nilearn

# Required by neuromaps.nulls.burt2020 (§7.4b, subcortical variogram null) — an optional
# install neuromaps does NOT bundle; without it burt2020() raises ImportError. We also
# call brainsmash.mapgen.Base directly for the subcortex (see src/nulls.py) since
# burt2020's volumetric path expects neuromaps' own MNI152 reference embedding, which
# is the wrong tool for a handful of DK subcortical parcels.
brainsmash

# Dev/test only.
pytest
